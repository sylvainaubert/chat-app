import React from "react";
import './Navbar.css';

function Navbar() {
    return (
        <nav className="Navbar">
            <a href="#">The Chatter</a>
        </nav>
    );
}

export default Navbar;